[project]
name = "atm_log_analyzer"
version = "0.0.1"
description = "atm_log_analyzer"
readme = "README.md"
requires-python = ">=3.12"

dependencies = [
    "pydantic>=2.10.0",
]
[tool.setuptools]
packages = ["app"]
[project.optional-dependencies]
dev = [
    "pytest",
    "black",
    "flake8"
]